# This project needs NO API secrets — your WhatsApp session lives in the paired auth/ dir.
#
# Optional overrides:
# WA_AUTH_DIR=./auth          # where the paired-session credentials are stored
# WA_STORE_DB=./store/wa.db   # local SQLite message store path
